{{#if session.isAuthenticated}}


{{else}}
  {{main-slider}}
  {{outlet}}

  {{#list-filter
   filter=(action 'filterByBook')
   as |model|}}
   <ul class="results">

          {{book-listing content=model}}

  </ul>
  {{/list-filter}}
{{/if}}
