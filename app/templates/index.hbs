{{#if session.isAuthenticated}}

{{else}}
  {{main-slider}}
  {{outlet}}

  {{#list-filter
   filter=(action 'filterByBook')
   as |model|}}
   <ul class="results">
      {{#each content as |model|}}
          {{book-listing book=model}}
      {{/each}}
  </ul>
  {{/list-filter}}
{{/if}}
